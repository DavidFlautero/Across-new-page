import "./globals.css";
import CookieConsent from "@/components/legal/CookieConsent";
import FloatingCallButton from "@/components/ui/FloatingCallButton";
import { Manrope, Inter_Tight } from "next/font/google";

export const metadata = {
  title: "Across Logistics",
  description:
    "Soluciones logísticas internacionales para transporte, aduanas, almacenaje, distribución y operaciones especiales.",
  icons: {
    icon: "/images/logo-ACROSS-Blanco.svg",
    shortcut: "/images/logo-ACROSS-Blanco.svg",
    apple: "/images/logo-ACROSS-Blanco.svg",
  },
};

const manrope = Manrope({
  subsets: ["latin"],
  variable: "--font-manrope",
});

const interTight = Inter_Tight({
  subsets: ["latin"],
  variable: "--font-tight",
});

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="es">
      <body
        className={`${manrope.variable} ${interTight.variable}`}
      >
        {children}
      <FloatingCallButton />
        <CookieConsent />
      </body>
    </html>
  );
}
