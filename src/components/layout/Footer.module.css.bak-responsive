.footer {
  background: linear-gradient(180deg, #0c0d12 0%, #05060a 100%);
  color: #e5e7eb;
  padding: 2rem 5vw 1rem;
  border-top: 1px solid rgba(255,255,255,0.08);
  font-family: inherit;
}

.inner {
  width: min(1220px, 100%);
  margin: 0 auto;
}

/* Layout escritorio: columnas en una fila compacta */
.top {
  display: grid;
  grid-template-columns: 220px 1fr 1fr 1fr 1fr 1fr;
  gap: 1.2rem;
  align-items: start;
}

/* Brand */
.brand {
  display: flex;
  flex-direction: column;
  gap: 0.8rem;
}
.brand img {
  width: 160px;
  height: auto;
}
.socials {
  display: flex;
  gap: 0.7rem;
  padding-top: 0;
  border-top: none;
}
.socials a {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: #e5e7eb;
  text-decoration: none;
  font-size: 0.9rem;
  font-weight: 500;
}
.email {
  font-size: 0.75rem;
}

/* Secciones */
.section {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}
.section h3 {
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: #fff;
  margin: 0 0 0.25rem 0;
  padding-bottom: 0.3rem;
  border-bottom: 1px solid rgba(255,255,255,0.2);
  display: inline-block;
}
/* Grid de oficinas: 2 columnas en desktop para que ocupe menos altura */
.officesGrid {
  display: grid;
  grid-template-columns: repeat(2, minmax(100px, 1fr));
  gap: 0.4rem 0.8rem;
}
.officesGrid p {
  margin: 0;
  font-size: 0.7rem;
  line-height: 1.3;
  color: #cbd5e1;
}
.officesGrid strong {
  display: block;
  font-weight: 700;
  color: #f1f5f9;
}
.officesGrid span {
  display: block;
  font-size: 0.65rem;
  color: #94a3b8;
}

/* Enlaces */
.linksList {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
}
.linksList a {
  color: #cbd5e1;
  text-decoration: none;
  font-size: 0.7rem;
  line-height: 1.3;
}
.linksList a:hover {
  color: #fff;
  text-decoration: underline;
  text-decoration-color: #d00032;
}

.bottom {
  margin-top: 1.5rem;
  padding-top: 1rem;
  border-top: 1px solid rgba(255,255,255,0.1);
  display: flex;
  justify-content: space-between;
  font-size: 0.65rem;
  color: #94a3b8;
}

/* Responsive tablet */
@media (max-width: 1024px) {
  .top {
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }
  .officesGrid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* Responsive móvil (acordeón) */
@media (max-width: 768px) {
  .footer {
    padding: 1.5rem 1rem 1rem;
  }
  .top {
    grid-template-columns: 1fr;
    gap: 0;
  }
  .brand {
    align-items: center;
    text-align: center;
    margin-bottom: 1rem;
    gap: 0.5rem;
  }
  .brand img {
    width: 140px;
  }
  .socials {
    justify-content: center;
  }
  .section {
    border-bottom: 1px solid rgba(255,255,255,0.1);
  }
  .section h3 {
    cursor: pointer;
    display: flex;
    justify-content: space-between;
    width: 100%;
    margin: 0;
    padding: 0.8rem 0;
    border-bottom: none;
    font-size: 0.85rem;
  }
  .section h3::after {
    content: "▼";
    font-size: 0.7rem;
    transition: transform 0.2s;
  }
  .section.open h3::after {
    transform: rotate(180deg);
  }
  .sectionContent {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-out;
  }
  .section.open .sectionContent {
    max-height: 600px;
    transition: max-height 0.4s ease-in;
    margin-bottom: 0.8rem;
  }
  .officesGrid {
    grid-template-columns: 1fr 1fr;
    gap: 0.6rem;
  }
  .linksList {
    gap: 0.6rem;
  }
  .linksList a {
    font-size: 0.8rem;
  }
}
@media (max-width: 480px) {
  .officesGrid {
    grid-template-columns: 1fr;
  }
}
